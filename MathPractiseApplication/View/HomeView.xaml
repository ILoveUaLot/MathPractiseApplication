<UserControl x:Class="MathPractiseApplication.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MathPractiseApplication.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Grid.Column="0"
                CornerRadius="20" Margin="20"
                >
            <Border.Background>
                <SolidColorBrush Color="{StaticResource ContentElementBackground}" Opacity="0.33"></SolidColorBrush>
            </Border.Background>
            <Border BorderBrush="{StaticResource MaterialDesignDarkBackground}"
                    BorderThickness="0,0,0,2"
                    Height="50"
                    VerticalAlignment="Top">
                <TextBlock FontSize="18"
                           FontWeight="Medium"
                           FontFamily="{StaticResource Montserrat}"
                           Foreground="White"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Text="Последний результат"></TextBlock>
            </Border>
        </Border>

        <Border Grid.Row="0" Grid.Column="1"
                CornerRadius="20" Margin="20"
                >
            <Border.Background>
                <SolidColorBrush Color="{StaticResource ContentElementBackground}" Opacity="0.33"></SolidColorBrush>
            </Border.Background>
            <Border BorderBrush="{StaticResource MaterialDesignDarkBackground}"
                    BorderThickness="0,0,0,2"
                    Height="50"
                    VerticalAlignment="Top">
                <TextBlock FontSize="18"
                           FontWeight="Medium"
                           FontFamily="{StaticResource Montserrat}"
                           Foreground="White"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Text="Практика"></TextBlock>
            </Border>
        </Border>

        <Border Grid.Row="0" Grid.Column="2"
                Grid.RowSpan="2"
                CornerRadius="20" Margin="20"
                >
            <Border.Background>
                <SolidColorBrush Color="{StaticResource ContentElementBackground}" Opacity="0.33"></SolidColorBrush>
            </Border.Background>
            <Border BorderBrush="{StaticResource MaterialDesignDarkBackground}"
                    BorderThickness="0,0,0,2"
                    Height="50"
                    VerticalAlignment="Top">
                <TextBlock FontSize="18"
                           FontWeight="Medium"
                           FontFamily="{StaticResource Montserrat}"
                           Foreground="White"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Text="Статистика"></TextBlock>
            </Border>
        </Border>
    </Grid>
</UserControl>
