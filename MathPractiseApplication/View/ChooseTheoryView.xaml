<UserControl x:Class="MathPractiseApplication.View.ChooseTheoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MathPractiseApplication.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             >
    <Grid>
        <StackPanel Orientation="Vertical">
            <Expander Style="{DynamicResource MaterialDesignExpander}"
                      Header="Решение уравнений"
                      Background="transparent"
                      Foreground="white">
                <StackPanel Orientation="Horizontal" Height="300">
                    
                    <Border Style="{StaticResource contentBorder}" Width="250">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50">

                                </RowDefinition>
                                <RowDefinition>

                                </RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Style="{StaticResource HeaderContentBorder}">
                                <TextBlock Style="{StaticResource HeaderContentText}" 
                                           Text="Линейные уравнения"></TextBlock>
                            </Border>
                            <Border Grid.Row="1" CornerRadius="0,0,20,20">
                                <Border.Background>
                                    <ImageBrush Stretch="Fill" ImageSource="/Images/Background.png" Opacity="0.7"/>
                                </Border.Background>
                            </Border>
                            <materialDesign:PackIcon Grid.Row="1"
                                                     Style="{StaticResource ContentImgInfo}"
                                                     ></materialDesign:PackIcon>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource contentBorder}" Width="250">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50">

                                </RowDefinition>
                                <RowDefinition>

                                </RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Style="{StaticResource HeaderContentBorder}">
                                <TextBlock Style="{StaticResource HeaderContentText}" 
                                           Text="Свойства функций"></TextBlock>
                            </Border>
                            <Border Grid.Row="1" CornerRadius="0,0,20,20">
                                <Border.Background>
                                    <ImageBrush Stretch="Fill" ImageSource="/Images/Background.png" Opacity="0.7"/>
                                </Border.Background>
                            </Border>
                            <materialDesign:PackIcon Grid.Row="1"
                                                     Style="{StaticResource ContentImgInfo}"
                                                     ></materialDesign:PackIcon>
                        </Grid>
                    </Border>
                </StackPanel>
            </Expander>
            <Expander Style="{DynamicResource MaterialDesignExpander}"
                      Header="Решение системы уравнений"
                      Background="transparent"
                      Foreground="white">
                <StackPanel Orientation="Horizontal" Height="300">

                    <Border Style="{StaticResource contentBorder}" Width="250">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50">

                                </RowDefinition>
                                <RowDefinition>

                                </RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Style="{StaticResource HeaderContentBorder}">
                                <TextBlock Style="{StaticResource HeaderContentText}" 
                                           Text="Система линейного уравнения" TextWrapping="Wrap"></TextBlock>
                            </Border>
                            <Border Grid.Row="1" CornerRadius="0,0,20,20">
                                <Border.Background>
                                    <ImageBrush Stretch="Fill" ImageSource="/Images/Background.png" Opacity="0.7"/>
                                </Border.Background>
                            </Border>
                            <materialDesign:PackIcon Grid.Row="1"
                                                     Style="{StaticResource ContentImgInfo}"
                                                     ></materialDesign:PackIcon>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource contentBorder}" Width="250">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50">

                                </RowDefinition>
                                <RowDefinition>

                                </RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Style="{StaticResource HeaderContentBorder}">
                                <TextBlock Style="{StaticResource HeaderContentText}" 
                                           Text="Методы решения"></TextBlock>
                            </Border>
                            <Border Grid.Row="1" CornerRadius="0,0,20,20">
                                <Border.Background>
                                    <ImageBrush Stretch="Fill" ImageSource="/Images/Background.png" Opacity="0.7"/>
                                </Border.Background>
                            </Border>
                            <materialDesign:PackIcon Grid.Row="1"
                                                     Style="{StaticResource ContentImgInfo}"
                                                     ></materialDesign:PackIcon>
                        </Grid>
                    </Border>
                </StackPanel>
            </Expander>
        </StackPanel>
        
    </Grid>
</UserControl>
